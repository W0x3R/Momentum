export const selectLanguageInner = document.querySelector('.language__inner')
export const selectImagesSrcWrapper = document.querySelector('.source')

export const arrowIconClose = (el) => {
	el.blur()
}